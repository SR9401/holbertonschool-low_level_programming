#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
	int main(void)
{
	int b10 = 48;
	int af = 97;

	while (b10 < 58)

{
	putchar(b10);

	b10++;
}
	while (af < 103)
{
	putchar(af);

	af++;
}
{
	putchar('\n');
}
return (0);
}

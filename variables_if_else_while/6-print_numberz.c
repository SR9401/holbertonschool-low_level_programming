#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
	int main(void)
{
	int b10 = 48;

	while (b10 < 58)
{
	putchar(b10);
	/*if (b10 < 58)*/

	b10++;
}
{
	putchar('\n');
}
return (0);
}

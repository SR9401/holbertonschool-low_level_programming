#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*liste de 0 a 19*/
	int main(void)
{
	int b10 = 48;

	while (b10 < 58)
{
	putchar(b10);
	if (b10 < 57)

	b10++;
}
	putchar('\n');
return (0);
}
